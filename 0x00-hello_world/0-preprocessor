#!/bin/bash
# Runs a C file through the preprocessor and saves the result into the file 'c'.

# Check if the CFILE variable is set
if [ -z "$CFILE" ]; then
  echo "Error: CFILE variable is not set."
  exit 1
fi

# Run the preprocessor
gcc -E "$CFILE" -o c
